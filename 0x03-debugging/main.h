#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>


void positive_or_negative(int i);
int largest_number(int a int b, int c);
int convert_day(in month, int day);
void print_remaining _days(int month, int day, int year);
#endif
